# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
# sqlalchemy 2.1.0 não existe como release estável; usamos 2.0.48, que é a última versão estável disponível e compatível com Python 3.13
sqlalchemy==2.0.48
alembic==1.13.1
# Driver PostgreSQL baseado em psycopg3 com binários incluídos (não depende de libpq.so do sistema, funcionando melhor no ambiente do Railway)
psycopg[binary]==3.1.18

# Data Validation
pydantic==2.10.4
pydantic-settings==2.7.0

# HTTP Client
httpx==0.26.0

# Scheduling
apscheduler==3.10.4

# Environment Variables
python-dotenv==1.0.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
hypothesis==6.98.3
